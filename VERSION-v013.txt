TexasSolverGPU production batch runner v013

- Preserves the v012 BB-response extraction path as the default.
- Adds decision-node selection for UTG c-bet analysis after BB checks.
- UTG_CBET exports UTG Check / Bet strategy and per-action EVs before applying the bet.
- Supports decision IDs in study output/dataset names and manifests.
- Uses the same verified WebView2 native bridge and original GPU/CUDA solver engine.
- No artificial board-count limit.
- Starts a fresh isolated native host for every board.
- Installs the WinEvent window suppressor before each host launch.
- Contains no TexasSolverGPU executable, DLL, or original runtime directory.
