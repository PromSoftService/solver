TexasSolverGPU production batch runner v012

- Simple interface: tsgpu-batch.cmd config.json boards.txt output\
- Automatically locates TexasSolverGpu_131.exe in the adjacent original folder.
- Uses the verified WebView2 native bridge and the existing GPU/CUDA engine.
- No artificial board-count limit.
- Starts a fresh isolated native host for every board.
- Installs a WinEvent window suppressor before each host launch.
- Writes per-board JSON/CSV and aggregate batch-summary.json/csv.
- Contains no TexasSolverGPU executable, DLL, or original runtime directory.
