name: CFG002 tiny report

on:
  push:
    branches: [main]
    paths:
      - 'tools/report_cfg002_tiny.py'
      - 'tools/report_cfg002_refine.py'
      - '.github/workflows/cfg002-tiny.yml'
  workflow_dispatch:

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print refinement report
        run: python3 tools/report_cfg002_refine.py
