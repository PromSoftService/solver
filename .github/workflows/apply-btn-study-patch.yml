name: Apply BTN follow-up study patch

on:
  push:
    branches: [main]
    paths:
      - 'tools/apply_btn_study_patch.py'
      - '.github/workflows/apply-btn-study-patch.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply main patch
        run: |
          set +e
          python3 tools/apply_btn_study_patch.py
          code=$?
          set -e
          echo "patch helper exit code: $code"
          grep -q "BTN_RESPONSE" tsgpu-worker.ps1
          grep -q "BTN_STAB" tsgpu-worker.ps1
          test -f scripts/Run-CFG003-NOD004-BRD001.ps1
          test -f scripts/Run-CFG003-NOD005-BRD001.ps1
          test -f scripts/RUN__CFG003__NOD004_NOD005__BRD001__AND_PUSH.cmd
      - name: Parse changed PowerShell
        shell: pwsh
        run: |
          $failed = $false
          foreach ($path in @(
            'tsgpu-worker.ps1',
            'tsgpu-batch.ps1',
            'scripts/Run-Study.ps1',
            'scripts/Run-CFG003-NOD004-BRD001.ps1',
            'scripts/Run-CFG003-NOD005-BRD001.ps1',
            'tools/collect_dataset.ps1'
          )) {
            $tokens = $null
            $errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $path), [ref]$tokens, [ref]$errors)
            if ($errors.Count -gt 0) {
              Write-Host "Parser errors in $path"
              $errors | ForEach-Object { Write-Host $_.Message }
              $failed = $true
            }
          }
          if ($failed) { exit 1 }
      - name: Check diff
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name "PromSoftService"
          git config user.email "97607535+PromSoftService@users.noreply.github.com"
          git add AGENTS.md README.md docs configs scripts tsgpu-worker.ps1 tsgpu-batch.ps1 tools/collect_dataset.ps1
          git commit -m "add BTN flop follow-up studies"
          git push
